name: pr-review-workflow
version: "1.0"
trigger: pull_request

steps:
  - id: fetch_diff
    description: Fetch the PR diff from GitHub event context

  - id: analyze
    skill: pr-review
    input: diff from fetch_diff

  - id: post_comment
    tool: github-api.post_pr_comment
    input: review from analyze